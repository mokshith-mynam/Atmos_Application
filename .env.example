DATABASE_URL=postgresql://atmos:atmos_dev@localhost:5432/atmos
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
ATMOS_API_URL=http://localhost:8000
SENSOR_FEED_URL=
WEATHER_FEED_URL=
GEE_REGION_BBOX=75.0,30.0,76.8,31.8
RASTER_OUTPUT_DIR=./data/raster

# Optional: create a free Google AI Studio key and configure it only on the API service.
# Never expose this value through VITE_* frontend variables or commit a real key.
GEMINI_API_KEY=

# Render frontend build-time variables. Replace with the deployed API URL.
VITE_API_URL=https://your-api-service.onrender.com
VITE_WS_URL=wss://your-api-service.onrender.com
